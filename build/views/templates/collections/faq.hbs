<div class="faqs cf">
  {{#each items}}
    <article class="faq cf">
      <h3><a href="{{url:item}}">{{question}}</a></h3>
      <p class="answer">{{{answer}}}</p>
    </article>
  {{/each}}

  {{collection:pager}}
</div>
